{
  "name": "weyland-fixture-nextjs",
  "private": true,
  "description": "Hello-world fixture for the Next.js lane (B88). Uses next/jest, Next's own jest transformer, so the fixture exercises the same transform pipeline a real Next app would rather than a hand-rolled babel config.",
  "scripts": {
    "test": "jest --testPathIgnorePatterns /node_modules/ /selfcheck/",
    "test:selfcheck": "jest selfcheck --testPathIgnorePatterns /node_modules/"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/react": "^16.1.0",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "next": "^16.3.3",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
