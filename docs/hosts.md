# Weyland — Host Inventory

Every machine/container in the lab. Keep updated as hosts change (see
[[feedback-keep-api-hosts-updated]]). Endpoints: [api.md](api.md).

> **IP confidence:** ✅ = confirmed this session (2026-06). ⚠ = from `weyland.md` (RE note, may be
> stale) — **verify and correct in place.**

| Host | Type | IP | Access | Role |
|---|---|---|---|---|
| **weyland** | Minisforum **MS-A2** — bare-metal Proxmox host (Ryzen 9 9955HX, 96 GB; **Proxmox VE 9.2.3**) | `192.168.1.232` ✅ | `root@weyland` | Proxmox infra: hosts the VMs + LXCs below. NOT a VM. |
| **mother** | VM (`vm-101`) on weyland — **64 GB / 12 vCPU** (grown 16/4 → 50Gi → **64Gi (B79, 2026-07-12** — reclaimed the 32 GB freed by decommissioning the Ollama CT-102; ended the chronic tight-node/OOM wall). k3s **max-pods raised 110→250** — `/etc/rancher/k3s/config.yaml` `kubelet-arg: max-pods=250` — after JupyterHub hit the default cap; **B99 2026-07-22: SWAP DISABLED** (`/swap.img` commented out of `/etc/fstab` — on this overcommitted single k8s node swap THRASHES under memory pressure and stalls the control plane/network into unreachable, the 2026-07-21 outage; proven by A/B test — swap-on reproduced it, swap-off survived extreme pressure via clean kernel-OOM) + kubelet memory **backstop** `system-reserved=2Gi`/`kube-reserved=1Gi`/`eviction-hard=memory.available<1.5Gi`; config codified at `nodes/mother/host/rancher/k3s/config.yaml`) | `192.168.1.243` ✅ | `emangini@mother` | k3s AI platform (tool-server, **weyland-guard** (B70 Part 1 — shared B14 guard service, `/guard/{input,output,act}`, models load once, fail-open; runbooks/guardrails.md), **weyland-agent** (agent.weyland.lab — B70 Part 3 agentic RAG: LangGraph loop + LlamaIndex retrievers + MLflow Traces; runbooks/agentic-rag.md), Postgres/pgvector, Qdrant + Weaviate (RAG backends **+ dataset VECTOR stores** B1: audio-feature (z-scored) + bge-text vectors, 9 datasets each — fma_features/echonest/uci/spotify/gtzan/lp_musiccaps/audioset/big_five; one build → both backends, collection/class per dataset), Neo4j (+APOC/GDS — RAG/AIDLC graphs **+ the music dataset GRAPH store** B1: genre tree · ~13.85M lastfm PLAYS · fma_tracks (BY/ON/IN_GENRE) · audioset; 2G heap/pagecache/5Gi; stays meshed via the `neo4j-bolt` DestinationRule TCP-keepalive — long Bolt bulk loads half-open behind Envoy otherwise), **NeoDash** NodePort 30088, Dagster, n8n, **docs-site** (docs.weyland.lab — B59 standalone MkDocs Material docs; replaced the **retired** Backstage IDP/TechDocs; initContainer builds → nginx serves, Mermaid renders; **Tools launchpad** at `docs.weyland.lab/tools/` — clickable directory of every `*.weyland.lab` UI, the reverse of DataHub's dataset→runbook Links, 2026-07-09), **LikeC4** (likec4.weyland.lab — **B64** interactive architecture explorer; node-built static SPA from `docs/architecture/weyland.likec4`, Keycloak forward-auth; the SAME model is embedded interactively in the C4 doc pages via `mkdocs-likec4`; runbooks/likec4.md), **MLflow** (mlflow.weyland.lab, B10+B16, **v3.14 — B47 upgrade from 2.18**: FastAPI/uvicorn, db-migrated, 4Gi, `--allowed-hosts` — + **LAN NodePort 30500** `mlflow-lan`, iptables-pinned to rogueone, for the external Ray worker; two-plane artifacts direct-to-MinIO), **Container registry** (registry.weyland.lab — MinIO-backed OCI `distribution/registry`, blobs in MinIO `registry` bucket, no-auth LAN, `k8s/registry/`, 2026-07-06), **Ray head** (ray.weyland.lab — persistent Ray cluster you submit training/HP-sweep jobs to, `k8s/ray/`, hostNetwork/plain-Ray, **Ray 2.56 + token auth — B47** (`RAY_AUTH_MODE=token`, Secret `ray-auth`, enforced 401); rogueone joins as a permanent native edge worker (Python 3.11.14 EXACT match), 2026-07-06; **Ray metrics** → Prometheus (head + rogueone-worker `:8080` static scrape) + Grafana `Dashboards → Ray`), **Registry UI** (registry-ui.weyland.lab — joxit, nginx-proxy → registry svc, `k8s/registry/registry-ui.yaml`), **Feast UI** (feast-ui.weyland.lab — feast registry browser, `feast ui` + registry.json-dump launcher for the 0.58 empty-UI bug, `k8s/data-mesh/feast-ui.yaml`), **Uptime Kuma** (kuma.weyland.lab, 37 monitors → Telegram paging; pod uses LAN CoreDNS + mounts mkcert CA), **Unleash** (unleash.weyland.lab — OSS feature flags, B43 feature-mgmt category; stateless, meshed to STRICT Postgres, → Port `feature_flag` webhook), **SonarQube** (sonarqube.weyland.lab — B43 code-quality; meshed Postgres; fed by the weekly `sonar-scan` CronJob — part of the 9-tool `code-scan-suite` → Port `code_quality`/`security_scan`/`code_hotspot` + Code Health dashboard), **KEDA** (ns `keda`, core + HTTP add-on — autoscaling/run-mode engine for the data mesh), **prometheus-pve-exporter** (ns `monitoring` — Proxmox metrics → Grafana #10347), **Loki + Alloy** (logs, ns `monitoring` → MinIO) + **Tempo** (traces, ns `monitoring` → MinIO) — **full LGTM in Grafana** (B48), **GlitchTip** (glitchtip.weyland.lab — B51 error tracking: web+worker+Valkey, meshed Postgres; tool-server+Dagster wired via Sentry SDK → Port `glitchtip_issue`), **Loki ruler** (LogQL log alerts → Alertmanager→Telegram), **OpenCost** (opencost.weyland.lab, ns `opencost` — B55 k8s cost allocation, custom on-prem pricing → Port Cloud Cost; reads the in-cluster Prometheus), **Woodpecker CI** (woodpecker.weyland.lab, ns `woodpecker` — B56 CI/CD, server + 2 agents, k8s backend (steps = cluster pods), GitHub OAuth, → Port `ci_pipeline`; LAN-only → manual/cron triggers), **Argo CD** (argocd.weyland.lab, ns `argocd` — B58 GitOps CD for the k8s layer; app-of-apps, **59 apps** onboarded (2026-07-20); deploy = push to git → reconcile), **Ranger** (ranger.weyland.lab, ns `data-mesh` — **B1.6 L5 Slice A** data-plane authz for Trino: column masking, default-deny; runbooks/ranger.md), **Gatekeeper Policy Manager** (gatekeeper.weyland.lab, ns `gatekeeper-system` — **B1.6 L5 Slice B** OPA admission Report UI + 6-panel Grafana dashboard), **Soda** (**B1.6 L5 Slice C** data-quality contract scan over the 7 marts — `soda_quality_job`, isolated `/opt/soda-venv` + trino-noauth proxy, results → DataHub Assertions; **no UI**; runbooks/soda.md), **Keycloak** (keycloak.weyland.lab, ns `weyland` — **B1.1 central IdP/SSO**, k8s + meshed Postgres, `weyland` realm; realm + OIDC clients codified in `tofu/keycloak/`; **every browser UI behind it** as of 2026-06-25 — OIDC ×3 + forward-auth for the rest), **traefik-forward-auth** (auth.weyland.lab, ns `weyland` — forward-auth SSO gate for ALL non-OIDC browser UIs (the whole `*.weyland.lab` UI set; not the S3 API / NodePort data backends / APISIX gateway, which are API-auth'd); cookie domain `weyland.lab`, single logout `/_oauth/logout`), **coredns-custom** (ns `kube-system` — forwards `weyland.lab` from cluster-DNS to the LAN resolver so in-cluster pods can reach `*.weyland.lab` back-channels; B1.1), **data-mesh** (ns `data-mesh` — **B1.2 L1 storage foundation**: Nessie (nessie.weyland.lab — Iceberg catalog + table versioning, Postgres `nessie` + MinIO `warehouse`) + lakeFS (lakefs.weyland.lab — file/dataset versioning, Postgres `lakefs` + MinIO `lakefs`) + **Trino** (trino.weyland.lab — B65 Tier-2 federation query engine, native-Nessie `iceberg` + `postgresql` catalogs; web UI monitoring-only, query via CLI/IntelliJ/Superset; in DataHub w/ lineage) + **GizmoSQL** (DuckDB Flight SQL, NodePort `mother:31337` — B65 Tier-2 #2: single-node OLAP; silver materialised as **persisted DuckDB tables** on a PVC, schema-per-domain `datasets_music`/`datasets_health` (tables not views — GizmoSQL `GetTables` doesn't surface views → IDE-tree-browsable now); IDEA via the Arrow Flight SQL JDBC driver; meshed, app runs plaintext + Istio mTLS in-cluster, gRPC-TLS ingress for external = B69) + **Superset** (superset.weyland.lab — B65 Tier-2 #3: BI/SQL exploration, Helm 0.17.2/Superset 6.1.0; Keycloak OIDC; shared Valkey cache; Trino + 11 Postgres DBs; in DataHub) + **dbt** (B1.5 transform tier — dbt-trino builds 7 tested marts `iceberg.dbt.mart_*` from the gold Iceberg via the Dagster `weyland_dbt_assets` job, weekly Sun 06:00; **dbt-docs.weyland.lab** = the model DAG/lineage/test UI (reuses the dagster image, meshed→Trino, forward-auth); the marts are the source of truth feeding Feast + the genre trainer (`--source mart`) + DataHub (custom `emit_dbt` + the native dbt connector reading `s3://warehouse/_dbt_artifacts/`)) + **Lightdash** (lightdash.weyland.lab — dbt-native BI over the marts, B1.5; helm 2.9.1, lab-Postgres `lightdash` DB, own login; browserless/NATS off for the tight node; connects to Trino via the **trino-noauth** proxy because Lightdash forces a password + no-auth Trino 401s Basic auth) + **trino-noauth** (tiny nginx proxy that strips the `Authorization` header for the Lightdash→Trino hop, `k8s/lightdash/trino-noauth-proxy.yaml`; Host must NOT be overridden or Istio Envoy 503s the outbound) + **Cube** (cube.weyland.lab — **B1.7 L6 semantic/metrics layer** over the marts; `cubejs/cube` (dev-mode bundled Cube Store), headless **SQL(:15432, pg-wire)/REST/GraphQL(:4000)** via the trino-noauth proxy; Keycloak forward-auth; Superset consumes it via `MEASURE()` virtual datasets; `k8s/cube/`, runbooks/cube.md) + **MetricFlow** (B1.7 dbt Semantic Layer — `mf query` compiles metrics from `dbt/models/semantic_models.yml` to Trino, spined by `metricflow_time_spine`; runs in the dagster image) + **Valkey** (shared data-mesh cache, RESP-compatible Redis fork, `valkey.data-mesh.svc:6379`) + **TimescaleDB** (B65 Tier-2 #4: time-series Postgres extension, `timescaledb.data-mesh.svc:5432`, db `timeseries`; 5 hypertables fed hourly by Dagster + **8 who_gho_* dataset hypertables** (WHO GHO country/year, hydrated 2026-07-01 by datasets_health_timescaledb_load; Last.fm skipped — no per-listen timestamps); Grafana datasource + Superset charts; in DataHub) + **MySQL** (B65 Tier-2 #5: health datasets, `mysql.data-mesh.svc:3306`; hydrated from silver Parquet 2026-07-01 — 6 DBs = grid MySQL=Y set nhanes/big_five/who_gho/cdc_physical_activity/brfss/nhis, 32 tables; usda/open_food_facts are MySQL=N; nhis replaced uk_biobank) + **MusicBrainz Postgres** (B65 Tier-2 #6: grid Postgres cell = MusicBrainz only, dedicated **postgres:18** `musicbrainz-postgres.data-mesh.svc:5432`, db musicbrainz_db/schema musicbrainz; loaded 2026-07-01 with the full native mbdump — 2.9M artists/39.3M recordings/1.1M links — via musicbrainz-docker's importer as a k8s Job, NOT stale mbslave; isolated from core weyland Postgres; user musicbrainz/musicbrainz superuser) + **MongoDB** (B65 Tier-2 #7: document store, always-on mongo:8 `mongodb.data-mesh.svc:27017`, authSource admin; loaded 2026-07-02 — who_gho 8 collections + open_food_facts 4.5M docs from silver Parquet + aidlc-kb 511 frontmatter docs; DataHub native Mongo source) + **CockroachDB** (B65 Tier-2 #8: distributed SQL, single-node insecure cockroach:v24.2.4 `cockroachdb.data-mesh.svc:26257`, Admin UI cockroachdb.weyland.lab forward-auth; loaded 2026-07-02 — brfss 6 tables ~3M rows + nhis, db per dataset pg-wire; `cockroachdb://` dialect not plain pg; DataHub native source) + **Cassandra** (B65 Tier-2 #9: wide-column, single-node cassandra:5.0 `cassandra.data-mesh.svc:9042`, 3G heap/6Gi; loaded 2026-07-02 — keyspaces datasets_music (uci + lastfm ~17M by user_id) + datasets_health (big_five/who_gho); partition-col + row_id uuid clustering, empty partition→__UNKNOWN__ sentinel; DataHub native source, profiling excl. lastfm) + **ClickHouse** (B65 Tier-2 #10: columnar OLAP, single-node clickhouse:24.8 `clickhouse.data-mesh.svc:8123`/`:9000`, 8Gi; `/play` UI clickhouse.weyland.lab; loaded 2026-07-02 — datasets_music (fma/uci/musicbrainz-subset/lp_musiccaps/audioset) + datasets_health (usda ~30 tables incl food_nutrient 26.8M, OFF) via **native s3() ingest** from lakeFS; `default` has a password (users.d Secret — DataHub sqlalchemy can't no-auth); DataHub native source) + **Redpanda** (B1.5 streaming data plane — Kafka-wire broker + built-in Schema Registry in one binary, single-node KRaft, `redpanda.data-mesh.svc:9092`/registry `:8081`; Console at redpanda.weyland.lab; serves the grid Redpanda column: Avro event topics `datasets.*` from the Dagster stream_produce producer + Debezium CDC `cdc.*`; isolated from DataHub's internal Kafka) + **Kafka Connect** (Debezium CDC worker, `kafka-connect.data-mesh.svc:8083`; Postgres connector on musicbrainz-postgres → change events; Confluent Avro converter injected via initContainer from cp-kafka-connect) + **Flink** (flink.weyland.lab — **B83 stream-processing tier**; Flink 1.20 K8s-operator **session cluster** `weyland-flink` (data-mesh, sidecar off, JM+TM, state/HA on MinIO `s3://warehouse/_flink`) + **flink-jars** (nginx serves `sql-runner.jar`+`health-job.jar` — session-mode jarURI must be http, not `local://`) + **History Server** flink-history.weyland.lab (archived jobs at `/jobs/overview`); 4 jobs across all Flink surfaces = RTA + CDC (Flink SQL → Iceberg `analytics.trending_artists` / upsert `datasets_music.cdc_demo_live`) + health (Java DataStream keyed-state → `analytics.health.state_risk`) + music (PyFlink Python-UDF, app-mode `weyland-flink-py` → upsert-kafka `analytics.music.artist_tier`); Prometheus `:9249`→ServiceMonitor; flame-graph profiling on; runbooks/flink.md) + **LanceDB** (grid LanceDB — EMBEDDED vector store, no server; tables on the lakeFS S3 gateway `s3://<repo>/main/lancedb`, same 9 vector sets as Qdrant/Weaviate; queried in-process; **Lance Data Viewer** browse UI at lancedb.weyland.lab fed by an event-triggered `mc mirror` → PVC (Dagster `lancedb_sync_sensor` fires on load; 6h CronJob backstop)) + **Feast** (grid Feast — feature store, B1.8: registry+offline = Postgres `feast` DB, online = Valkey; **feast-server** REST at feast.weyland.lab (`/docs` Swagger; slim image; MESHED in data-mesh for STRICT-mTLS Postgres) serves online features by entity key; point-in-time training retrieval via SDK; 2 views — spotify audio features + brfss chronic-condition prevalence) + **Port actions→cluster** (ns `port-agent` — outbound-polling `port-labs/port-agent` + `store-scaler` svc in data-mesh: a Port self-service action scales data-mesh deployments; reusable Port→cluster button path, 2026-07-02; sleep-feature parked on Argo selfHeal — see runbooks/port-agent-easy-button.md) + **RAG streaming indexer** (B-RAG-STREAM - the `rag_stream_produce` Dagster producer chunks+embeds (on rogueone `rag-embed` :8900) then publishes to the Redpanda topic **`rag.chunks`** (Confluent-Avro, key `source_path`, `upsert`+`delete`-tombstone); **5 one-image consumers** `weyland-rag-index:local` (`STORE` env): `rag-index-{qdrant,weaviate,opensearch}` ns `data-mesh` (sidecar OFF) + `rag-index-{pgvector,neo4j}` ns `weyland` (sidecar ON, Kafka ports 9092/8081 excluded); change-detection/prune via the **`rag_manifest`** Postgres table (`source_path` PK + `content_hash`) — decoupled from pgvector `rag_documents`; retired the in-process `chunks`/`embeddings`/`hash_check` + 5 `*_write` assets), all meshed to STRICT Postgres; forward-auth UIs, in-cluster svcs for CLI/pipelines), Grafana (+ Loki/Tempo/Alertmanager datasources), MinIO, CoreDNS, Traefik, **Promptfoo** (promptfoo.weyland.lab, ns `weyland` — **B84 P2c** prompt-regression eval gate; self-hosted server (`promptfoo view` default CMD, port 3000), evals run via `kubectl exec … promptfoo eval`, shared `/data` PVC, forward-auth; `k8s/promptfoo/`, demos/eval-lanes.md), **LiteLLM model gateway** NodePort 30400, **Bifrost** (bifrost.weyland.lab — B17+B19 Ph3b agent-edge MCP front door; `maximhq/bifrost` aggregates the compositor's 91 fleet tools → one `/mcp` endpoint for coding agents (Cline/Cursor/Claude Code); UI `/` forward-auth, `/mcp` un-gated (MCP clients can't browser-SSO); reads only, acts still gateway→`/mcp-act`; `k8s/bifrost/`), **Istio service mesh** (B8 done — istiod + Kiali (traces→Tempo); tool-server + 4 backends + Dagster meshed, PERMISSIVE mTLS, Postgres STRICT; **Jaeger retired B48**)), **JupyterHub** (jupyter.weyland.lab, ns `jupyterhub` — **B1.8 L8 data-science notebooks**; Zero-to-JupyterHub, hub+proxy tiny always-on + KubeSpawner singleuser pods **on-demand** (1h idle-cull); Keycloak OIDC (`GenericOAuthenticator`, mkcert-CA back-channel); custom `weyland-jupyter:local` image (polars/s3fs/pylance/duckdb, `ctr import`ed, pullPolicy Never) querying lakeFS; singleuser NetworkPolicy `privateIPs:true` to reach the mesh; runbooks/jupyterhub.md). Hosts the **AIDLC knowledge corpus** (B37, MinIO `aidlc-kb` → RAG + Neo4j `:Entry` graph). Host tuning: `fs.inotify.max_user_instances=512` + `vm.max_map_count=524288` (`/etc/sysctl.d/`; inotify exhausted by Istio sidecars, max_map_count for SonarQube ES). qemu-guest-agent installed (clean Proxmox shutdowns). |
| **rogueone** | Lenovo ThinkPad P16 laptop (external; RTX 5000 Ada 16 GB, 128 GB RAM) | `192.168.1.230` ✅ | `edwardmangini@rogueone` | GPU inference — **vLLM on-demand bench** (B111: `:8001`, `Qwen2.5-7B-AWQ`, continuous-batching throughput; runs on the **native Docker engine** not Desktop; `scripts/vllm-bench.sh {start,stop}`, Bifrost `vllm` provider; [runbooks/gpu-inference.md](runbooks/gpu-inference.md)) + **Ollama** (B79, 2026-07-12 — moved off weyland CT-102; `:11434` LAN-bound via `OLLAMA_HOST=0.0.0.0`, serves the eval-judge panel + tool-server/open-webui + the B66 operator brain; the cluster reaches it at `192.168.1.230:11434` / `ollama.weyland.lab`) + primary dev workstation + Claude Code; **permanent native Ray edge worker** (`ray-worker.service` → mother's Ray head; not-always-up, auto-rejoins on wake; exports Ray metrics on `:8080`, statically scraped by the in-cluster Prometheus) + **remote model training** (`genre-trainer` container via Docker Desktop) + **rag-embed** (B-RAG-STREAM warm embedding service — native systemd `rag-embed.service`, `192.168.1.230:8900`, `bge-base-en-v1.5` (768-dim, L2-normalized, B74) resident on the RTX 5000 Ada; `GET /health` + `POST /embed`; sole client = the Dagster `rag_stream_produce` producer). Obsidian notes (no longer a RAG source, B25b). |
| ~~**ollama**~~ **DELETED** | ~~LXC **CT 102** on weyland~~ — decommissioned B79 (2026-07-12), **CT destroyed 2026-07-18** (`pct destroy 102`) | ~~`192.168.1.244`~~ | — | Ollama **runs on rogueone** (`192.168.1.230:11434`, `ollama.weyland.lab` → `.230` via LAN DNS). CT-102 was moved off to free 32 GB → grow mother 50→64 GB. [runbooks/model-serving-ollama.md](runbooks/model-serving-ollama.md) |
| **whisper** | LXC **CT 103** on weyland (unprivileged) | `192.168.1.246` ✅ (DHCP-reserved) | `pct enter 103` (from weyland host) | whisper.cpp CPU STT — shim `/v1` @ :9000, native `/inference` @ :8080. [runbooks/transcription-whisper.md](runbooks/transcription-whisper.md) |
| ~~**hermes**~~ **RETIRED** | ~~LXC **CT 104** on weyland~~ — shut off 2026-07-20, **destroyed 2026-07-23** (`pct destroy 104`) | ~~`192.168.1.247`~~ | — | Hermes agent platform (B2, NousResearch framework) — **decommissioned.** Its role (text-it → it-acts operator) is replaced by the **B66 operator agent**, a LangGraph pod on **mother** (`gpt-oss:20b` brain, Telegram long-poll, tool-server `/mcp-act`). Kanban (B27) retired earlier → Linear. |

## Access notes
- **Per-host SSH users differ:** `root@weyland`, `emangini@mother`,
  `edwardmangini@rogueone`. Always use **hostnames**, not IPs, in commands (except standalone CTs,
  which aren't in DNS — use their reserved IPs or the new `*.weyland.lab` names).
- **LXC containers have no direct login** — reach them through the weyland host: `ssh root@weyland`
  → `pct enter <ctid>`. Push files in with `pct push <ctid> <host-file> <ct-path>`.
- **DNS:** `*.weyland.lab` resolves via CoreDNS on `mother:53` (k3s/Traefik UIs → mother;
  `ollama` → **rogueone `.230`** (B79 moved it off CT-102), `whisper` → its CT IP). rogueone resolves `*.weyland.lab` via **`/etc/hosts`** (per-host
  entries) — so a **new** subdomain (e.g. `auth.weyland.lab`) needs its own `/etc/hosts` line until the
  workstation is pointed at the wildcard LAN DNS. **In-cluster pods** resolve `weyland.lab` via the
  `coredns-custom` forward (kube-system → LAN resolver), added in B1.1 so SSO back-channels work.

## Container/VM map on weyland (Proxmox)
```text
weyland (MS-A2, Proxmox)
├── vm-101  mother     (k3s platform)
├── ct-102  ollama     (RETIRED B79 → moved to rogueone; 32 GB reclaimed for mother)
├── ct-103  whisper    (STT serving)
└── ct-104  hermes     (RETIRED 2026-07-23 → destroyed; B66 operator replaces it, runs on mother k8s)
```
