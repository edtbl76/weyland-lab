# C4 Container — Weyland

Level 2: node topology inside the MS-A2 — the mother VM (k3s) plus the LXC containers, and how they wire to the external clouds. Interactive; drill into a node in the [explorer](https://likec4.weyland.lab). For mother's internals see [c4-component-mother.md](c4-component-mother.md).

```likec4-view topology
```
