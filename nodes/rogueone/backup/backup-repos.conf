# B130 — git repos to back up (ALLOW-LIST).
#
# Only repos listed here are backed up. Every other git repo on rogueone is
# IGNORED COMPLETELY (committed code is recoverable from its remote anyway).
#
# For each listed repo the backup captures `git ls-files --others` — untracked
# WIP + gitignored local files (.env, local data, secrets) — MINUS the
# reproducible bulk in restic-excludes.txt (node_modules, build/, venvs, …).
# Committed/tracked files are skipped (they live on GitHub).
#
# One repo path per line; ~ expands to $HOME. Blank lines and #-comments ignored.

~/Documents/Studio/STUD.io
~/IdeaProjects/weyland
~/PycharmProjects/Algopedia
~/IdeaProjects/ServiceTransformation
