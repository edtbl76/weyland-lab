"""Fixture validators subpackage — the planted violation lives in a SUBMODULE (bad.py) so a broken import
proves the real contract's package-level `source_modules = guardrails` covers guardrails.validators.*."""
