# Generated by scripts/gen_odcs_contract.py from Trino + Soda + dbt substance (B157). Enrich the description prose by hand.
apiVersion: v3.0.0
kind: DataContract
id: weyland-music-genre-taxonomy
name: Genre Taxonomy
version: 1.0.0
status: active
domain: music
dataProduct: Genre Taxonomy
tenant: weyland
description:
  purpose: FMA genre taxonomy dimension — genre_id, title, parent (id + title), and a top-level flag.
    One row per genre.
  usage: Query the iceberg.dbt.mart_fma_genre_tree table in Trino; see docs/demos/music-domain.md for
    worked queries and the BI surfaces.
servers:
- server: trino-dbt
  type: trino
  catalog: iceberg
  schema: dbt
  physicalName: iceberg.dbt.mart_fma_genre_tree
schema:
- name: mart_fma_genre_tree
  physicalName: iceberg.dbt.mart_fma_genre_tree
  properties:
  - name: genre_id
    logicalType: integer
    physicalType: bigint
    description: FMA genre id (unique).
  - name: genre_title
    logicalType: string
    physicalType: varchar
    description: Genre name.
  - name: parent_id
    logicalType: integer
    physicalType: bigint
    description: Parent genre id (null / 0 for top-level).
  - name: parent_title
    logicalType: string
    physicalType: varchar
    description: Parent genre name (null for top-level).
  - name: is_top_level
    logicalType: boolean
    physicalType: boolean
    description: True for root genres.
quality:
- property: table
  rule: row_count > 0
- property: table
  rule: duplicate_count(genre_id) = 0
- property: table
  rule: missing_count(genre_title) = 0
slaProperties:
- property: frequency
  value: on-demand
- property: retention
  value: indefinite
team:
- role: owner
  username: ed@timberbacklabs.com
support:
- channel: runbook
  url: docs/demos/music-domain.md
customProperties:
- property: medallion_layer
  value: gold
- property: source_system
  value: FMA
