# B78 (EMA-69) — test-only deps for weyland-dagster, installed per-project by scripts/run-lang-tests.sh.
# The MINIMAL set the loader / parquet-read tests need — deliberately NOT requirements.txt, which pulls
# the whole dagster + torch + sentence-transformers runtime into the fast CI test lane. Pinned to the
# runtime versions in requirements.txt so the tests exercise the same behaviour the pipeline runs.
pyarrow==25.0.0
pandas==2.3.3
numpy==2.4.6
minio==7.2.20
import-linter==2.15
hypothesis==6.140.3
# datahub_emit builds real MetadataChangeProposals; testing them means asserting the REAL aspects the SDK
# produces (not stub objects), so the emitter's own package is a test dep — pinned to the runtime version.
# It is the heaviest test dep (pulls avro/pydantic); it earns its place by making the 3k-line emitter testable.
acryl-datahub==1.6.0.15
# sqlglot: datahub_emit's dbt column-lineage tracing parses SQL with it; the _trace_dbt_columns test needs it,
# and without it here that test silently skips in the scan lane (importorskip) — losing real coverage.
sqlglot
# sqlalchemy: the mysql/cockroach store-loaders write via pandas.to_sql(engine); the tests drive them against a
# real in-memory sqlite engine and read the rows back, so it's a genuine write-path test (no live MySQL/CRDB).
sqlalchemy
