# Finance volatility trainer (B113 Phase 5). Top-level deps; pip resolves the transitive set at build. See
# requirements.in for the rationale (a lean list, not genre-trainer's inconsistent 130-line freeze).
mlflow
scikit-learn
pandas
numpy
minio
pyarrow
boto3
ray[default]
