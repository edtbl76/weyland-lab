# rag-embed service (rogueone, GPU). torch is installed SEPARATELY with the CUDA wheel already present in
# rogueone's training venvs — pin here only the service deps so we don't clobber the CUDA torch build.
fastapi
uvicorn[standard]
pydantic>=2
sentence-transformers
