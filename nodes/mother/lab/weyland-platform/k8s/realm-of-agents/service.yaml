apiVersion: v1
kind: Service
metadata:
  name: realm-of-agents
  namespace: weyland
  labels:
    app: realm-of-agents   # selected by the ServiceMonitor
spec:
  type: ClusterIP
  selector:
    app: realm-of-agents
  ports:
    - name: http
      port: 8080
      targetPort: 8080
