# Template only — the real secret is applied out-of-band (kubectl) and NEVER committed. Argo excludes this file.
#   client-secret = tofu output -raw traefik_forward_auth_client_secret
#   cookie-secret = openssl rand -hex 32
apiVersion: v1
kind: Secret
metadata:
  name: traefik-forward-auth-secret
  namespace: weyland
type: Opaque
stringData:
  client-secret: CHANGE_ME
  cookie-secret: CHANGE_ME
