# Single-node OpenSearch 3.x — LAN-internal exposed search/log playground (B1.3).
# NOT used by DataHub (DataHub runs its own bundled Elasticsearch 7.x). Security plugin OFF.
clusterName: opensearch-cluster
nodeGroup: master
singleNode: true
replicas: 1
roles:
  - master
  - ingest
  - data
  - remote_cluster_client

# 1 GB heap — two search instances on the node now, keep it lean.
opensearchJavaOpts: "-Xms1g -Xmx1g"

resources:
  requests:
    cpu: "50m"           # low reservation — playground, near-idle; node CPU requests run ~98% committed
    memory: "2Gi"
  limits:
    memory: "3Gi"        # heap must be <= 50% of this

persistence:
  enabled: true
  size: 20Gi             # storageClass omitted -> k3s default local-path

config:
  opensearch.yml: |
    cluster.name: opensearch-cluster
    network.host: 0.0.0.0
    discovery.type: single-node
    plugins.security.disabled: true
    # Report ES 7.10.2 at the root endpoint so Elasticsearch clients (DataGrip's driver) accept the
    # connection — OpenSearch's own version string is version-gated out. Static node setting (not dynamic).
    compatibility.override_main_response_version: true

extraEnvs:
  - name: DISABLE_INSTALL_DEMO_CONFIG
    value: "true"
  - name: DISABLE_SECURITY_PLUGIN
    value: "true"
