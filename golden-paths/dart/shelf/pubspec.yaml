name: golden_dart_shelf
description: >-
  Golden path — server-side Dart / shelf (B160). The idiomatic shelf + shelf_router
  baseline. Runnable, ephemeral, extendable. Conforms to the golden-path contract
  (docs/design/golden-paths.md): GET /health /ready /metrics /hello.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.5.0

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.1.4

dev_dependencies:
  test: ^1.25.0
  lints: ^4.0.0
