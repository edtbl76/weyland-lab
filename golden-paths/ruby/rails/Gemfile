# Golden path — Ruby / Rails (B160). Contract: /health /ready /metrics /hello.
# API-only, minimal railties: no Active Record, no Action View — just routing + controllers.
source "https://rubygems.org"

# Rails 8: ActiveSupport 8 dropped the `quirks_mode: true` argument to JSON.generate that 7.2 still
# passed, so `render json:` works with the current json 3.x gem without pinning json back to 2.x.
gem "rails", "~> 8.0"
gem "puma", "~> 6.4"

group :test do
  gem "rake"
  gem "rubocop", "~> 1.65", require: false
end
